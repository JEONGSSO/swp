<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.js.swp.mapper.BoardMapper">

	<insert id="create">
		insert into tb1_board(title, content, writer)
		values(
		#{title}, #{content}, #{writer})
	</insert>

	<select id="read" resultType="com.js.swp.domain.Board">
		select bno, title, content, writer, regdate, viewcnt, replycnt
		from tb1_board
		where bno = #{bno}
	</select>

	<update id="update">
		update tb1_board set title =#{title},
		content = #{content}
		where bno = #{bno}
	</update>
	
	<delete id="delete">
		delete from tb1_board where bno = #{bno}
	</delete>
	
		<select id="getMaxbno" resultType = "Integer">
		select max(bno) from tb1_board
	</select>

	<select id="listAll" resultType="com.js.swp.domain.Board">
		<![CDATA[
		select
			bno, title, content, writer, regdate, viewcnt, replycnt
		from tb1_board
		where bno > 0
		order by bno desc, regdate desc
		]]>
	</select>

	<select id="listPage" resultType="com.js.swp.domain.Board">
			select
				bno, title, content, writer, regdate, viewcnt, replycnt
			from tb1_board
			where bno > 0
				order by bno desc, regdate desc
				limit #{page}, 10
	</select>
	
	<sql id="search">
				<if test = "searchType != null">
						<if test = "searchType == 't'.toString()">
							and title like CONCAT('%', #{keyword}, '%')
						</if>
					
						<if test = "searchType == 'c'.toString()">
							and content like CONCAT('%', #{keyword}, '%')
						</if>
					
						<if test = "searchType == 'w'.toString()">
							and writer like CONCAT('%', #{keyword}, '%')
						</if>
					
						<if test = "searchType == 'tc'.toString()">
								and( title like CONCAT('%', #{keyword}, '%')
								or writer like CONCAT('%', #{keyword}, '%'))
						</if>
					
						<if test = "searchType == 'a'.toString()">
								and( title like CONCAT('%', #{keyword}, '%')
								or title like CONCAT('%', #{keyword}, '%')
								or content like CONCAT('%', #{keyword}, '%')
								or writer like CONCAT('%', #{keyword}, '%'))
						</if>
				</if>
		</sql>
	
	<select id="listCriteria" resultType="com.js.swp.domain.Board">
		select
			bno, title, content, writer, regdate, viewcnt, replycnt
		from tb1_board
			where bno > 0
		<include refid="search"></include>
		order by bno desc, regdate desc
			limit #{pageStart}, #{perPageNum}
	</select>
	
	<select id="countPaging" resultType="int">
		<![CDATA[
		select
			count(bno)
		from tb1_board
			tb1_board
		where
			bno > 0
		]]>
	</select>
	
		<!-- 0918  -->
	<update id="plusViewcnt">
		update tb1_board set viewcnt = viewcnt + 1
		where bno = #{bno}
	</update>
	
	<update id="updateReplycnt">
		update tb1_board set replycnt = replycnt + #{amt},
		where bno = #{bno}
	</update>
		<!-- 0918 까지  -->
		<!--  
	<update id="updateCnt">
		update tb1_board  set replycnt = (select count(*) where bno = #{bno})
		where #{bno} >0
	</update>
	-->
	

	
</mapper>